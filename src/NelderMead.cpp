#include "NelderMead.hpp"

#include <cstring>   // memcpy
#include <iostream>  // cout, endl, bad_alloc

#ifndef DBL_MAX
#include <limits>  // numeric_limits
#define DBL_MAX std::numeric_limits<double>::max()
#endif

NelderMead::NelderMead(int dim, double error) : m_dim(dim), m_error(error) {
  if (dim < 1) {
    throw std::invalid_argument("Dimensional Value Invalid! Set dim > 0.");
  }
  if (error < 0) {
    throw std::invalid_argument("Invalid m_error! Set error >= 0.");
  }
  m_nPoints = (dim > 2) ? (dim + 1) : 3;

  try {
    m_points = new double *[m_nPoints];
    m_costs = new double[m_nPoints];
    m_centroid = new double[m_dim];
    m_bestPoint = new double[m_dim];
    m_pointReflection = new double[m_dim];   // point generated by reflection
    m_pointExpansion = new double[m_dim];    // point generated by expansion
    m_pointContraction = new double[m_dim];  // point generated by contraction
    for (int i = 0; i < m_nPoints; i++) {
      m_points[i] = new double[m_dim];
    }
  } catch (const std::bad_alloc &e) {
    std::cout << "Memory Allocation"
              << " is failed: " << e.what() << std::endl;
  }

  m_worstID = m_nPoints - 1;   // index of the worst solution
  m_secondID = m_nPoints - 2;  // index of the second worst solution
  m_bestID = 0;                // index of the best solution

  m_costReflection = 0.0;   // Cost of reflected point
  m_costExpansion = 0.0;    // Cost of expanded point
  m_costContraction = 0.0;  // Cost of contracted point

  m_alpha = A_LFA;  // Used in reflection
  m_gamma = G_AMA;  // Used in expansion
  m_rho = R_HO;     // Used in contraction
  m_sigma = S_GMA;  // Used in reduction

  m_result = 0.0;
  m_iPoint = 0;
  m_step = 0;

  for (int i = 0; i < m_nPoints; i++) {
    m_costs[i] = DBL_MAX;
  }

  // Generate inital m_points
  srand48_r(time(NULL), &buffer);  // random seed
  for (int i = 0; i < m_nPoints; i++) {
    for (int j = 0; j < m_dim; j++) {
      drand48_r(&buffer, &m_result);            // random number in 'm_result'
      m_points[i][j] = (m_result * 2.0 - 1.0);  // numbers between -1 and 1.
    }
  }
}

double *NelderMead::run(double _cost) {
  do {
    switch (m_step) {
      case 0:  // Initialize Points
        if (m_iPoint > 0) {
          m_costs[m_iPoint - 1] = _cost;
        }
        if (m_iPoint < m_nPoints) {
          m_iPoint++;
          return m_points[m_iPoint - 1];
        }

      case 1:  // Initialize Variables
        sort_points();
        calculate_centroid();

      case 2:  // Reflection - Use m_alpha
        for (int j = 0; j < m_dim; j++) {
          m_pointReflection[j] =
              fmod(m_centroid[j] + m_alpha * (m_centroid[j] - m_points[m_worstID][j]), 1);
        }
        m_step = 3;
        return m_pointReflection;

      case 3:
        m_costReflection = _cost;
        if (m_costs[m_bestID] <= m_costReflection && m_costReflection < m_costs[m_secondID]) {
          swap(m_pointReflection, m_points[m_worstID]);
          m_costs[m_worstID] = m_costReflection;
          break;
        } else if (m_costReflection < m_costs[m_bestID]) {
          // Calculate Expansion - Use m_gamma
          for (int j = 0; j < m_dim; j++) {
            m_pointExpansion[j] =
                fmod(m_centroid[j] + m_gamma * (m_pointReflection[j] - m_centroid[j]), 1);
          }
          m_step = 4;
          return m_pointExpansion;
        } else if (m_costs[m_secondID] <= m_costReflection &&
                   m_costReflection < m_costs[m_worstID]) {
          // Calculate Outside Contraction - Use m_rho
          for (int j = 0; j < m_dim; j++) {
            m_pointContraction[j] =
                fmod(m_centroid[j] + m_rho * (m_pointReflection[j] - m_centroid[j]), 1);
          }
          m_step = 5;
          return m_pointContraction;
        } else if (m_costReflection >= m_costs[m_worstID]) {
          // Calculate Inside Contraction - Use m_rho
          for (int j = 0; j < m_dim; j++) {
            m_pointContraction[j] =
                fmod(m_centroid[j] - m_rho * (m_centroid[j]) - m_points[m_worstID][j], 1);
          }
          m_step = 6;
          return m_pointContraction;
        }

      case 4:
        m_costExpansion = _cost;
        if (m_costExpansion < m_costReflection) {
          swap(m_pointExpansion, m_points[m_worstID]);
          m_costs[m_worstID] = m_costExpansion;
        } else {
          swap(m_pointReflection, m_points[m_worstID]);
          m_costs[m_worstID] = m_costReflection;
        }
        break;

      case 5:
        m_costContraction = _cost;
        if (m_costContraction <= m_costReflection) {
          swap(m_pointContraction, m_points[m_worstID]);
          m_costs[m_worstID] = m_costContraction;
        } else {
          m_step = 7;
        }
        break;

      case 6:
        m_costContraction = _cost;
        if (m_costContraction < m_costs[m_worstID]) {
          swap(m_pointContraction, m_points[m_worstID]);
          m_costs[m_worstID] = m_costContraction;
          break;
        } else {
          m_step = 7;
          break;
        }
    }

    sort_points();
    if (m_step == 7) {
      // Replace all m_points, except the m_bestPoint
      for (int i = 0; i < m_nPoints; i++) {
        if (i != m_bestID) {
          for (int j = 0; j < m_dim; j++) {
            m_points[i][j] =
                fmod(m_points[m_bestID][j] + m_sigma * (m_points[i][j] - m_points[m_bestID][j]), 1);
          }
        }
      }
      m_step = 0;
      m_iPoint = 1;
    }

    calculate_centroid();
    m_step = 2;

  } while (volume() > m_error);

  m_step = END;
  memcpy(m_bestPoint, m_points[m_bestID], m_dim * sizeof(double));

  return m_bestPoint;
}

void NelderMead::sort_points() {
  // Put in order solutions, ie, m_points[0][x] is the best, ...,
  // m_points[n+1][x] is the worst Bubble sort
  for (int i = 0; i < m_nPoints - 1; i++) {
    for (int j = i + 1; j < m_nPoints; j++) {
      if (m_costs[i] > m_costs[j]) {
        swap(m_points[i], m_points[j]);
        swap(m_costs[i], m_costs[j]);
      }
    }
  }
}

void NelderMead::calculate_centroid() {
  // Calculate m_centroid
  for (int j = 0; j < m_dim; j++) {
    m_result = 0.0;
    m_centroid[j] = 0.0;  // Centroid = {0}
    for (int i = 0; i < m_nPoints; i++) {
      // The worst solution is not necessary
      if (i != m_worstID) {
        m_centroid[j] += m_points[i][j];
        m_result += 1.0;
      }
    }
    m_centroid[j] /= (m_result);
  }
}

void NelderMead::reset(int level) {
  m_costReflection = 0.0;   // Cost of reflected point
  m_costExpansion = 0.0;    // Cost of expanded point
  m_costContraction = 0.0;  // Cost of contracted point

  m_result = 0.0;
  m_iPoint = 0;
  m_step = 0;

  sort_points();
  switch (level) {
    case 0:  // Reset with random m_points removing the best point
      for (int j = 0; j < m_dim; j++) {
        drand48_r(&buffer, &m_result);                   // random number in 'm_result'
        m_points[m_bestID][j] = (m_result * 2.0 - 1.0);  // numbers between -1 and 1.
      }
    case 1:  // Reset with random m_points keeping the best point
      for (int i = 0; i < m_nPoints; i++) {
        if (i != m_bestID) {
          for (int j = 0; j < m_dim; j++) {
            drand48_r(&buffer, &m_result);            // random number in 'm_result'
            m_points[i][j] = (m_result * 2.0 - 1.0);  // numbers between -1 and 1.
          }
        }
      }
      break;

    default:
      throw std::runtime_error("There is not the Nelder-Mead reset option level " +
                               std::to_string(level));
      break;
  }
}

double NelderMead::volume() {
  double total = 0.0;
  for (int i = 0; i < m_nPoints; i++) {
    double value = 0.0;
    for (int j = 0; j < m_dim; j++) {
      value += pow(m_points[i][j] - m_centroid[j], 2.0);
    }
    value = sqrt(value);      // With this calculation, 'value' is the norm.
    value = pow(value, 2.0);  // value is equal to norm²
    total += value;
  }
  return sqrt(total / m_nPoints);
}

void NelderMead::swap(double *&p1, double *&p2) {
  double *temp = p1;
  p1 = p2;
  p2 = temp;
}

void NelderMead::swap(double &p1, double &p2) {
  double temp = p1;
  p1 = p2;
  p2 = temp;
}

NelderMead::~NelderMead() {
  for (int i = 0; i < m_nPoints; i++) {
    delete[] m_points[i];
  }
  delete[] m_points;
  delete[] m_costs;
  delete[] m_centroid;
  delete[] m_bestPoint;
  delete[] m_pointReflection;
  delete[] m_pointExpansion;
  delete[] m_pointContraction;
}